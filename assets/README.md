# assets

Put the hero photo here as **`hero.jpg`** (the girl with the tablet — same image you sent).

- Recommended size: ~1200×1050 px, JPG or WebP (rename to `hero.jpg`).
- If the file is missing, the page automatically shows a light SVG placeholder instead — nothing breaks.
